_schema-version: 3.3.0
ID: Invoice_Management
description: A simple CAP project.
version: 1.0.0
modules:
- name: Invoice_Management-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Invoice_Management-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: Invoice_Management-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: Invoice_Management-db
  parameters:
    buildpack: nodejs_buildpack
- name: Invoice_Management-destination-content
  type: com.sap.application.content
  requires:
  - name: Invoice_Management-destination-service
    parameters:
      content-target: true
  - name: Invoice_Management_html_repo_host
    parameters:
      service-key:
        name: Invoice_Management_html_repo_host-key
  - name: uaa_Invoice_Management
    parameters:
      service-key:
        name: uaa_Invoice_Management-key
  parameters:
    content:
      instance:
        destinations:
        - Name: intinv_Invoice_Management_html_repo_host
          ServiceInstanceName: Invoice_Management-html5-app-host-service
          ServiceKeyName: Invoice_Management_html_repo_host-key
          sap.cloud.service: intinv
        - Authentication: OAuth2UserTokenExchange
          Name: intinv_uaa_Invoice_Management
          ServiceInstanceName: Invoice_Management-xsuaa-service
          ServiceKeyName: uaa_Invoice_Management-key
          sap.cloud.service: intinv
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: Invoice_Management-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Invoice_Management_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - invmanagement.zip
      name: invmanagement
      target-path: resources/
- name: invmanagement
  type: html5
  path: app/invmanagement
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Invoice_Management-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: Invoice_Management-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Invoice_Management-destination-service
    service-plan: lite
- name: Invoice_Management_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Invoice_Management-html5-app-host-service
    service-plan: app-host
- name: uaa_Invoice_Management
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: Invoice_Management-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
